let s: string = 'hey';
console.log(s);

let len: number;
len =7;
console.log(Math.trunc(len/2)); // 3 ( it removes/ ignores the decimal points)
console.log(Math.round(len/2)); // 4 (nearest integer value);
